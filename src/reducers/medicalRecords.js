const initialState = {
  medicalRecords: [],
};

const medicalRecords = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};
export default medicalRecords;
